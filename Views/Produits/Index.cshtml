@model List<EcommerceProject.Models.Produit>

<!-- Hero Section -->
<section class="hero bg-dark text-white text-center py-5" style="position: relative;">
    <div class="overlay"></div>
    <img src="/images/image.png" alt="Techno" class="hero-background" />
    <div class="container h-100 d-flex flex-column justify-content-center align-items-center text-shadow">
        <h1 class="display-2 fw-bold mb-4">Bienvenue à <span class="text-primary">Mekog</span></h1>
        <p class="fs-4 mb-4">Des produits exceptionnels, des prix incroyables. Profitez-en maintenant !</p>
        <a href="/account/shop" class="btn btn-primary btn-lg px-4 py-2 rounded-pill shadow-lg">Shop Now</a>
    </div>
</section>





<!-- Categories Section -->
<section class="categories py-5">
    <div class="container">
        <h2 class="text-center mb-5 fw-bold display-4 text-dark">Explorez Nos Catégories</h2>
        <div class="row g-4">
            <!-- Category Card with Image -->
            <div class="col-lg-3 col-md-4 col-sm-6">
                <div class="category-card d-flex flex-column justify-content-center align-items-center shadow-lg rounded py-4">
                    <div class="icon-container mb-3">
                        <img src="/images/camera-icon.png" alt="Caméras" class="img-fluid" style="max-height: 80px; object-fit: contain;">
                    </div>
                    <h5 class="fw-bold text-secondary">Caméras</h5>
                </div>
            </div>
            <!-- Category Card with Image -->
            <div class="col-lg-3 col-md-4 col-sm-6">
                <div class="category-card d-flex flex-column justify-content-center align-items-center shadow-lg rounded py-4">
                    <div class="icon-container mb-3">
                        <img src="/images/phone-icon.png" alt="Téléphones" class="img-fluid" style="max-height: 80px; object-fit: contain;">
                    </div>
                    <h5 class="fw-bold text-secondary">Téléphones</h5>
                </div>
            </div>
            <!-- Category Card with Image -->
            <div class="col-lg-3 col-md-4 col-sm-6">
                <div class="category-card d-flex flex-column justify-content-center align-items-center shadow-lg rounded py-4">
                    <div class="icon-container mb-3">
                        <img src="/images/laptop-icon.png" alt="PC & Laptops" class="img-fluid" style="max-height: 80px; object-fit: contain;">
                    </div>
                    <h5 class="fw-bold text-secondary">PC & Laptops</h5>
                </div>
            </div>
            <!-- Category Card with Image -->
            <div class="col-lg-3 col-md-4 col-sm-6">
                <div class="category-card d-flex flex-column justify-content-center align-items-center shadow-lg rounded py-4">
                    <div class="icon-container mb-3">
                        <img src="/images/headphone-icon.png" alt="Écouteurs" class="img-fluid" style="max-height: 80px; object-fit: contain;">
                    </div>
                    <h5 class="fw-bold text-secondary">Écouteurs</h5>
                </div>
            </div>
        </div>
    </div>
</section>


<!-- Flash Deals Section -->
<section class="flash-deals py-5">
    <div class="container">
        <h2 class="text-center mb-5 fw-bold display-3 text-primary">Offres Flash Exclusives</h2>
        <div class="row g-5">
            @foreach (var produit in Model)
            {
                <div class="col-lg-4 col-md-6">
                    <div class="product-card position-relative shadow-lg rounded-3 overflow-hidden bg-white">
                        <!-- Image Produit -->
                        <div class="product-image position-relative">
                            <img src="@produit.ImageUrl" alt="@produit.Nom" class="img-fluid w-100">
                            <div class="badge-discount position-absolute top-0 start-0 bg-danger text-white px-3 py-1 rounded-end">
                                -20%
                            </div>
                            <div class="badge-stock position-absolute bottom-0 end-0 bg-warning text-dark px-3 py-1 rounded-start">
                                Stock Limité
                            </div>
                        </div>
                        <!-- Détails Produit -->
                        <div class="product-info p-4 text-center">
                            <h5 class="fw-bold text-dark mb-2 text-truncate" title="@produit.Nom">@produit.Nom</h5>
                            <div class="rating mb-2">
                                <i class="fas fa-star text-warning"></i>
                                <i class="fas fa-star text-warning"></i>
                                <i class="fas fa-star text-warning"></i>
                                <i class="fas fa-star-half-alt text-warning"></i>
                                <i class="far fa-star text-muted"></i>
                            </div>
                            <p class="text-muted small text-truncate" title="@produit.Description">@produit.Description</p>
                            <div class="price-container d-flex justify-content-between align-items-center mt-3">
                                <span class="text-danger fw-bold h4">@produit.Prix €</span>
<div class="product-action-buttons d-flex align-items-center justify-content-center mt-4">
    <a href="/Produits/Details/@produit.Id" class="btn btn-details">🔍 View Details</a>
    <a href="/Produits/AjouterAuPanier/@produit.Id" class="btn btn-add">🛒 Ajouter</a>
</div>



                            </div>
                        </div>
                    </div>
                </div>
            }
        </div>
    </div>
</section>

<style>
   /* Section Offres Flash */
   .flash-deals h2 {
       font-size: 3rem;
       font-weight: bold;
       margin-bottom: 40px;
       color: #0056b3;
       letter-spacing: 1.5px;
   }

   /* Carte produit */
   .product-card {
       background-color: #fff;
       border-radius: 15px;
       transition: transform 0.3s ease, box-shadow 0.3s ease;
       position: relative;
       overflow: hidden;
       height: 100%;
   }

   .product-card:hover {
       transform: translateY(-10px);
       box-shadow: 0 20px 30px rgba(0, 0, 0, 0.1);
   }

   /* Image du produit */
   .product-image {
       position: relative;
       height: 250px;
       overflow: hidden;
   }

   .product-image img {
       width: 100%;
       height: 100%;
       object-fit: cover;
       transition: transform 0.3s ease;
   }

   .product-card:hover .product-image img {
       transform: scale(1.1);
   }

   /* Badge de réduction */
   .badge-discount {
       font-size: 0.9rem;
       font-weight: bold;
       border-radius: 0 10px 10px 0;
       z-index: 1;
   }

   .badge-stock {
       font-size: 0.9rem;
       font-weight: bold;
       border-radius: 10px 0 0 10px;
       z-index: 1;
   }

   /* Informations sur le produit */
   .product-info {
       padding: 15px;
       text-align: center;
   }

   .product-info h5 {
       font-size: 1.2rem;
       font-weight: 700;
       margin-bottom: 10px;
       color: #333;
   }

   .product-info p {
       font-size: 0.9rem;
       margin-bottom: 15px;
       color: #777;
   }

   /* Boutons */
   .product-info .btn {
       font-size: 0.85rem;
       font-weight: bold;
       padding: 8px 20px;
       transition: all 0.3s ease;
   }

   .btn-primary {
       background-color: #007bff;
       border-color: #007bff;
   }

   .btn-primary:hover {
       background-color: #0056b3;
       border-color: #0056b3;
   }

   .btn-outline-primary {
       border: 2px solid #007bff;
       color: #007bff;
   }

   .btn-outline-primary:hover {
       background-color: #007bff;
       color: white;
   }

   /* Animation des boutons */
   .btn:hover {
       opacity: 0.8;
   }

   /* Responsive */
   @@media (max-width: 768px) {
       .product-card {
           margin-bottom: 30px;
       }

       .product-info h5 {
           font-size: 1rem;
       }

       .product-info p {
           font-size: 0.85rem;
       }
   }

 /* Hero Section */
.hero {
    position: relative;
    overflow: hidden;
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
    animation: fadeInSection 1.5s ease-in-out forwards; /* Section fade-in animation */
}

/* Full-Section Background Image */
.hero-background {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    object-fit: cover; /* Ensure the image covers the entire section */
    z-index: 0; /* Place it behind content */
    animation: zoomInBackground 12s infinite alternate; /* Smooth zoom animation */
}

/* Overlay */
.hero .overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 0, 0, 0.7); /* Dark overlay for better text visibility */
    z-index: 1;
    animation: fadeInOverlay 1.5s ease-in-out forwards;
}

/* Text Content */
.hero .container {
    position: relative;
    z-index: 2; /* Place content above the background and overlay */
    text-align: center;
    animation: slideInText 1.5s ease-out forwards; /* Text slide-in animation */
}

/* Button Styles */
.hero .btn-primary {
    margin-top: 20px;
    background: linear-gradient(45deg, #ff7e5f, #feb47b);
    border: none;
    padding: 0.8rem 2rem;
    border-radius: 50px;
    box-shadow: 0 8px 15px rgba(0, 0, 0, 0.3);
    transition: transform 0.3s ease, box-shadow 0.3s ease;
}

.hero .btn-primary:hover {
    transform: translateY(-5px) scale(1.1);
    box-shadow: 0 10px 20px rgba(0, 0, 0, 0.5);
}

/* Animations */
@@keyframes fadeInUp {
    0% {
        opacity: 0;
        transform: translateY(20px);
    }
    100% {
        opacity: 1;
        transform: translateY(0);
    }
}

.animated.fadeInUp {
    animation: fadeInUp 1s ease-out;
}

/* Text styles */
.hero h1 {
    font-size: 3.5rem;
    font-weight: bold;
    letter-spacing: 2px;
    text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5); /* Adding shadow to make text stand out */
}

.hero p {
    font-size: 1.5rem;
    margin-bottom: 30px;
    letter-spacing: 1px;
    text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5); /* Adding shadow to make text stand out */
}

/* Button styles */
.hero .btn {
    padding: 12px 30px;
    font-size: 1.1rem;
    border-radius: 30px;
    box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
    transition: background-color 0.3s ease;
}

.hero .btn:hover {
    background-color: #003366;
    transform: translateY(-5px); /* Adding hover effect to the button */
}

/* Responsive Design */
@@media (max-width: 768px) {
    .hero h1 {
        font-size: 2.5rem;
    }

    .hero p {
        font-size: 1.2rem;
    }

    .hero .btn {
        font-size: 1rem;
    }
}

/* Fade-in Effect for Section */
.categories {
    opacity: 0;
    transform: translateY(50px);
    animation: fadeIn 1.5s ease-in-out forwards;
}

@@keyframes fadeIn {
    to {
        opacity: 1;
        transform: translateY(0);
    }
}

/* Category Card Hover Effects */
.category-card {
    transition: transform 0.3s ease, box-shadow 0.3s ease;
    background: white;
}

.category-card:hover {
    transform: scale(1.05);
    box-shadow: 0 10px 20px rgba(0, 0, 0, 0.3);
}

/* Icon Bounce Effect on Hover */
.icon-container img {
    transition: transform 0.3s ease;
}

.icon-container img:hover {
    transform: scale(1.2) rotate(5deg);
}

/* Responsive Spacing */
@@media (max-width: 576px) {
    .category-card {
        padding: 1rem;
    }
}





/* Hero Section Styles */
.hero {
    position: relative;
    overflow: hidden;
    min-height: 100vh;
    display: flex;
    flex-direction: column; /* Stack content vertically */
    align-items: center; /* Center horizontally */
    justify-content: center; /* Center vertically */
    color: white;
    background-attachment: fixed;
    background-size: cover;
    background-position: center;
}

/* Button Position Below Text */
.hero .btn-primary {
    margin-top: 20px; /* Add spacing between text and button */
    background: linear-gradient(45deg, #ff7e5f, #feb47b); /* Vibrant gradient */
    border: none;
    padding: 0.8rem 2rem;
    border-radius: 50px;
    box-shadow: 0 8px 15px rgba(0, 0, 0, 0.3);
    transition: transform 0.3s ease, box-shadow 0.3s ease;
    z-index: 2; /* Ensure it's above the overlay */
}

/* Button Hover Effect */
.hero .btn-primary:hover {
    transform: translateY(-5px) scale(1.1); /* Lift and enlarge on hover */
    box-shadow: 0 10px 20px rgba(0, 0, 0, 0.5);
}

/* Overlay to Darken Background */
.hero .overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 0, 0, 0.7); /* Stronger dark overlay */
    z-index: 1;
}

/* Text Centering */
.hero h1,
.hero p {
    z-index: 2; /* Ensure they appear above the overlay */
    text-align: center;
}


/* Keyframe for Text Visibility and Fade-Out */
@@keyframes fadeOutText {
    0% {
        opacity: 1; /* Fully visible */
    }
    83.33% { /* After 10 seconds (10s / 12s = ~83%) */
        opacity: 1;
    }
    100% {
        opacity: 0; /* Fully invisible */
    }
}

/* Button Hover Effect */
.hero .btn-primary {
    background: linear-gradient(45deg, #ff7e5f, #feb47b);
    border: none;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
    z-index: 3;
}

.hero .btn-primary:hover {
    transform: translateY(-5px) scale(1.1);
    box-shadow: 0 8px 15px rgba(0, 0, 0, 0.5);
}

/* Responsive Design */
@@media (max-width: 768px) {
    .hero h1 {
        font-size: 2.5rem;
    }

    .hero p {
        font-size: 1.2rem;
    }

    .hero .btn-primary {
        padding: 0.8rem 2rem;
    }
}







</style>
<style>
    /* Button Group */
    .product-action-buttons {
        gap: 20px; /* Space between buttons */
    }

    /* Common Button Styles */
    .product-action-buttons .btn {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        font-size: 16px;
        font-weight: bold;
        padding: 15px 30px;
        border-radius: 50px; /* Fully rounded */
        text-transform: uppercase;
        text-decoration: none;
        color: white; /* Default text color */
        position: relative;
        overflow: hidden;
        transition: all 0.3s ease-in-out;
        box-shadow: 0 8px 15px rgba(0, 0, 0, 0.2); /* Subtle shadow */
    }

    /* View Details Button */
    .product-action-buttons .btn-details {
        background: linear-gradient(135deg, #ff512f, #dd2476); /* Orange to pink gradient */
    }

    .product-action-buttons .btn-details:hover {
        background: linear-gradient(135deg, #dd2476, #ff512f); /* Reverse gradient on hover */
        transform: translateY(-3px) scale(1.05); /* Lift and zoom on hover */
        box-shadow: 0 12px 20px rgba(255, 81, 47, 0.4); /* Stronger shadow */
    }

    /* Ajouter Button */
    .product-action-buttons .btn-add {
        background: linear-gradient(135deg, #00c6ff, #0072ff); /* Cyan to blue gradient */
    }

    .product-action-buttons .btn-add:hover {
        background: linear-gradient(135deg, #0072ff, #00c6ff); /* Reverse gradient on hover */
        transform: translateY(-3px) scale(1.05); /* Lift and zoom on hover */
        box-shadow: 0 12px 20px rgba(0, 198, 255, 0.4); /* Stronger shadow */
    }

    /* Button Icon Styling */
    .product-action-buttons .btn::before {
        content: '';
        position: absolute;
        top: 0;
        left: -100%;
        width: 300%;
        height: 100%;
        background: rgba(255, 255, 255, 0.15); /* Light shimmer effect */
        transform: skewX(-45deg); /* Angled shimmer */
        transition: all 0.5s ease-in-out;
        z-index: 1;
    }

    .product-action-buttons .btn:hover::before {
        left: 0; /* Slide effect */
    }

    /* Responsive Design */
    @@media (max-width: 768px) {
        .product-action-buttons {
            flex-direction: column; /* Stack buttons */
            gap: 15px; /* Space between stacked buttons */
        }

        .product-action-buttons .btn {
            width: 100%; /* Full-width on mobile */
        }
    }
</style>
